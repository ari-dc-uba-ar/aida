name: MiPrimerWorkflow

on:
  push:
    branches:
      - '*'

jobs:
  saludo:
    runs-on: ubuntu-latest

    steps:
      - name: Saludar por consola
        run: echo "Hola! Este es mi primer pipeline"
